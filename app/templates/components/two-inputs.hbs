<input
  type="text"
  class="lowercase-field"
  value={{str}}
  oninput={{action (mut str) value='target.value'}} />
<input
  type="text"
  class="uppercase-field"
  value={{uppercase}}
  oninput={{action (mut uppercase) value='target.value'}} />
