
import { makeName } from 'emberli/helpers/make-name';
import { module, test } from 'qunit';

module('Unit | Helper | make name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeName(['mike', 'north'], {});
  assert.ok(result);
});

