<h3>course.hbs</h3>

{{link-to '< Back' 'index'}}

<h1 class='course-title'>{{model.title}}</h1>

<p>
  {{model.summary}}
</p>

<ul class='stages'>
  {{#each model.stages as |stage|}}
    <li data-stage={{stage.slug}}>{{stage.title}}</li>
  {{/each}}
</ul>

{{outlet}}
